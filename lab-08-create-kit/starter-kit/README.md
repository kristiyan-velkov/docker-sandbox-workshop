# Kit mixin — starter for Lab 8 (copy and customize)

Copy this folder to `my-workshop-kit/` at repo root (or anywhere) and edit `spec.yaml`:

```bash
cp -r lab-08-create-kit/starter-kit ./my-workshop-kit
```

Then follow [../README.md](../README.md).
