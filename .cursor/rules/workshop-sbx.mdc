---
description: Docker Sandboxes (sbx) workshop context
alwaysApply: true
---

# Docker Sandbox Workshop

2-hour core session + **6 self-paced labs** on [Docker Sandboxes](https://docs.docker.com/ai/sandboxes/).

## Structure

- `lab-01-first-sandbox/` — install, run, inspect, teardown
- `lab-02-network-policy/` — network policy, custom template, clone preview
- `lab-03-secrets/` — GitHub credential injection, sentinel values
- `lab-04-clone-workflow/` — direct & clone mode on workshop-app / monorepo
- `lab-05-workshop-app/` — pre-built kit + network allow/deny demo
- `lab-06-customize-stack/` — create your custom kit — **final lab**
- `workshop-app/` — Next.js **playground** — sbx workspace for labs 4–6
- `docker-sandbox-platform/` — hosted register / login / progress — https://nextjs-26f1-3000.prg1.zerops.app
- `workshop-landing/` — Zerops conference site
- `customize/` — templates (`templates/`) and kit mixin (`kit/`)

## Key concepts

- **`sbx run cursor .`** — boot microVM, sync workspace, start agent
- **`sbx run --clone`** — private Git clone; fetch `sandbox-<name>` on host ([workflows](https://docs.docker.com/ai/sandboxes/workflows/))
- **`sbx ls`** — list sandboxes — no `sbx status`
- **Kits** — `--kit ./path` at create; bootstrap via `files/home/` + `commands.startup`; `sbx kit validate`
- **Secrets** — `sbx secret set -g cursor` (Lab 1); `sbx secret set -g github` (Lab 3)

## When editing lab content

Keep commands copy-pasteable in README markdown only. No lab driver shell scripts. Validate against https://docs.docker.com/ai/sandboxes/ and `sbx --help`.
